import React from "react";

function Settings() {
  return (
    <section>
      <h1>Profile Setting</h1>
      <input type="password" />
      <button>Change</button>
    </section>
  );
}

export default Settings;
